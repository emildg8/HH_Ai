# Режим без платного LLM — локальная оценка по ключевым словам.
# install.ps1 копирует это в config/secrets.local.env при первом запуске.
# Сменить режим: npm run setup  или docs/CONFIG-GUIDE.md

HH_SCORE_MODE=local-first
HH_LLM_MAX_PER_RUN=0
HH_OPENROUTER_MAX_CALLS_PER_RUN=0
