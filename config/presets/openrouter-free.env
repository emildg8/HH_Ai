# OpenRouter — бесплатные модели (рекомендуется для старта)
# Скопируйте в config/secrets.local.env и подставьте ключ с https://openrouter.ai

OpenRouter_API_KEY=sk-or-v1-ВСТАВЬТЕ_КЛЮЧ
OPENROUTER_MODEL=openrouter/free

# В config/profiles/devops.env (или ваш профиль):
# HH_LLM_MAX_PER_RUN=30
# HH_OPENROUTER_MAX_CALLS_PER_RUN=30
