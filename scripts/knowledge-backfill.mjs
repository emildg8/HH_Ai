/**
 * Заглушка backfill: импорт истории из queue/feedback — фаза B.
 *   node scripts/knowledge-backfill.mjs
 */
import { loadEnv } from '../lib/load-env.mjs';

loadEnv();

console.log('knowledge-backfill: ещё не реализован (фаза B плана). Запустите: npm run devops:knowledge-init');
