/**
 * Коды выхода hh-apply-chat-letter.mjs (для батча и обвязки).
 */
/** Отклик намеренно не отправлен: есть анкета работодателя, запись обновлена. */
export const HH_APPLY_EXIT_QUESTIONNAIRE_DEFERRED = 5;
/** Уже отклик / приглашение на hh.ru — повторный отклик не нужен. */
export const HH_APPLY_EXIT_ALREADY_RESPONDED = 6;
