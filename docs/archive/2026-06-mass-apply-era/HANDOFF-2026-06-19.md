# Handoff — 19 июня 2026 (сессия: gate/knowledge, employer dossier, батч UI)

## С чего начать следующей сессии

Сказать агенту одно из:

1. **Employer dossier** — `npm run devops:knowledge-sync-employers`, API `/api/employers`, `/api/employers/komitas`
2. **Автоотклик не идёт** — precheck «готово: 0», 52× «письмо не утверждено» → **«Перегенерировать (52)»** (если решите гонять батч)
3. **Проверить harvest** — CV на месте, кнопка «Запустить поиск» (Ctrl+Shift+R)
4. **Закоммить** накопленный diff — только по явной просьбе

---

## Сделано в этой сессии

### Этап 2 — employer dossier (срез)

| Компонент | Файлы |
|-----------|--------|
| Dossier + sync | `lib/employer-dossier.mjs` |
| CLI sync | `npm run devops:knowledge-sync-employers` |
| API | `GET /api/employers`, `GET /api/employers/:id` |
| Baseline hook | `writeIntelligenceBaseline` → `employerSync` в JSON |
| Тесты | `npm run test:employer-dossier` |

Синхронизировано **17** профилей работодателей из очереди в SQLite (из 484 с нулевыми откликами).

### Фаза A + срез gate / dual-write

| Компонент | Файлы |
|-----------|--------|
| Data root + SQLite | `lib/data-root.mjs`, `lib/knowledge-store.mjs`, `migrations/knowledge/` |
| Единый gate в батче | `lib/apply-gate.mjs` (`relaxedTargeting`, `skipRedFlags`), `scripts/hh-apply-batch.mjs` → `decideApplyGate` |
| Precheck API | `scripts/dashboard-server.mjs` — один `previewApplyGate` с `requireLetter: true` |
| Dual-write откликов | `lib/knowledge-apply-record.mjs` при `knowledgeStoreEnabled` |
| Маппинг skip | `lib/batch-gate-skip.mjs` |
| Тесты | `test-batch-gate-parity.mjs`, `test-knowledge-apply-record.mjs` в `package.json` |

### Baseline и Knowledge Store включены

```powershell
npm run devops:intelligence-baseline   # выполнено 19.06 ~17:00 UTC
npm run devops:knowledge-init        # schema v2, data/hh-ai-knowledge.db
```

- `config/preferences.json`: `applyIntelligence.knowledgeStoreEnabled: true`
- Baseline: **17 откликов**, invite **5.9%**, отказы **70.6%**, gate `minGateScore: 70`, `batchLetterMinScore10: 8`
- Снимок: `data/intelligence-baseline.json`
- `apply_attempts` в БД: **0** (батч с успешными откликами ещё не гоняли)

### Перенос из `D:\Dev\career`

Источник: `D:\Dev\career\hh-ru-apply.farm-old` (симлинк `hh-ru-apply` битый).

| Скопировано в `D:\Dev\apps\hh-ai` |
|-----------------------------------|
| `CV/` (md, pdf, exports, archive) |
| `config/cover-letter.txt` |
| `config/resume-routing.json` |
| `config/resume-variants.json` |
| `config/devops.env` |

**Не трогали:** `data/session` (в hh-ai новее: ~6295 vs ~5148 файлов).

Проверка: `npm run setup:check` — CV 4 файла, текст ~20k символов.

### UI дашборда

| Фикс | Где |
|------|-----|
| Кнопка сбора рядом с периодом | `dashboard/public/index.html` — `btn-run-harvest` в блоке `harvestPeriod` |
| Футер модалки настроек (ранее) | лишний `</div>` в секции «Письма» |
| Модалка precheck батча | `batch-precheck-body` + sticky footer, подсказка «сначала Перегенерировать», primary на regen |

### Из прошлых сессий (в diff, не повторять)

- Settings layout v9 (`settings9`)
- Автопроцессы + скрытый cmd (follow-up off by default)
- Copilot / interview hub (большой unstaged diff)

---

## Текущее состояние пользователя

| Область | Статус |
|---------|--------|
| Harvest | CV ок; кнопка «Запустить поиск» слева у «Период поиска» |
| Автоотклики ≥55 | Precheck: **58 кандидатов, 0 готово** — блокер: **52× качество письма** («письмо не утверждено») |
| Следующий шаг UX | **Перегенерировать (52)** в модалке precheck → дождаться LLM → снова «Запустить серию» |
| Knowledge | Включён, записей пока нет |
| Коммит | **Не делался** |

### Настройки откликов (актуальные)

```json
"batchAutoPrepareLetters": true,
"batchAutoApproveBestLetter": true,
"batchLetterMinScore10": 8,
"applyIntelligence": {
  "enabled": true,
  "minGateScore": 70,
  "knowledgeStoreEnabled": true
}
```

Автоподготовка **не создаёт** письма с нуля — только улучшает уже утверждённые. Для пустых карточек нужна **перегенерация** (LLM).

---

## Проверено

```powershell
cd D:\Dev\apps\hh-ai
npm run test:apply-gate          # OK
npm run test:knowledge-store   # OK
npm run setup:check            # CV, cover-letter, session OK
```

---

## Следующий шаг по плану «Отклики → Интервью»

1. **UI read-only:** chip работодателя на карточке (score, invite%)
2. **Этап 2.5:** `employer-rag` — контекст для LLM
3. **Опционально:** перегенерация писем → батч → `apply_attempts > 0` (вы не планируете батч на бою)
4. **Не делать пока:** полный backfill, export zip, фаза B целиком

План Cursor: `отклики_→_интервью_e597ecd8.plan.md`

---

## Git / коммит

Большой unstaged diff. **Не коммитить:** `data/`, `.env`, `config/devops.env`, `config/cover-letter.txt`, `CV/`, `hh-ai-knowledge.db`.

---

## Ключевые пути

| Путь | Назначение |
|------|------------|
| `D:\Dev\apps\hh-ai` | Текущий проект |
| `D:\Dev\career\hh-ru-apply.farm-old` | Старый бэкап (CV уже скопирован) |
| `data/intelligence-baseline.json` | Точка отсчёта воронки |
| `data/hh-ai-knowledge.db` | Knowledge Store |
| `data/harvest-run.log` | Лог сбора |
| Дашборд | http://127.0.0.1:3849 |

---

## Контекст для агента

- Транскрипт сессии: `agent-transcripts/b0b6d926-1ee7-46c4-8a50-caf2947ce306.jsonl`
- Правила: `AGENTS.md`, `docs/HIRING-OPERATIONS.md`, `docs/OPS-RHYTHM.md`
- Сайдбар: `sources: false` в preferences — панель «Источники» скрыта; harvest в `harvestPeriod`
