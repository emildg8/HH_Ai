# Предложения по улучшению (по диалогу 22.05.2026)

## Уже сделано

- Bundled Chromium + `HH_PLAYWRIGHT_CHANNEL` закомментирован в `.env`.
- Сайдбар: вкладки **Рутина / Работа / Отчёты / Ещё**, режим **Кратко / Полный**.
- Резюме: 5 hash в `resume-variants.json`, routing (`support`, `support_lead`, `tam`, `data`, `devops`), `devops:sync-resume-from-source`, `devops:probe-resume-completeness`.
- Импорт откликов hh → очередь (кнопка в «Отчёты»).
- Шаблоны ответов в чат на карточке; фильтр откликов по этапу во вкладке «Отклики».

## Приоритет 1 (осталось)

1. **Связать переписки hh с очередью** — импорт из кэша есть; после sync: `devops:apply-negotiations-cache`.
2. **После транскрибации** — `npm run devops:analyze-interviews` → подсказки в «К собесу» и черновиках чата.
3. **5 резюме** — hash в конфиге есть; периодически `devops:sync-resume-from-source` при смене эталона.
4. **Лимиты hh** — cap в настройках; пауза при капче — вынести в toast рутины.

## Приоритет 2

5. **Воронка по этапам** — отдельные вкладки: «Просмотрели», «Ждём ответа», «Приглашение», с быстрым фильтром и SLA (дней без ответа).
6. **Шаблоны ответов в чат** — 5–10 заготовок (зарплата, созвон, релокация) + LLM только дорабатывает.
7. **Анкеты** — очередь «только вопросы без ответа», автозапуск prep-batch после harvest для high-score.
8. **Telegram-дайджест** — раз в вечер: N новых, M откликов, K приглашений (скрипт уже есть для scan-tg — расширить).

## Приоритет 3

9. **A/B резюме** — метрика просмотр/приглашение по `resumeRouting.role` в конверсии.
10. **Реплика собеседований** — RAG по `data/interview-transcripts` в `interview-prep` и ответах в чат.
11. ~~**Публичный релиз 2.0.1**~~ — [v2.0.1](https://github.com/emildg8/HH_Ai/releases/tag/v2.0.1), FIRST-RUN, install, скрины.

## Риски

- Параллельный harvest + батч + sync — один профиль Chromium; рутина последовательная именно поэтому.
- LLM без ключа — черновики чата и анкет только fallback; держите OpenRouter или Ollama в `secrets.local.env`.
