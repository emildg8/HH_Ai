# Handoff — 20 июня 2026 (пауза: коммиты + employer-rag)

## С чего начать после паузы

Сказать агенту:

1. **Продолжить план** — UI превью employer-rag в письме (незакоммичено) или интервью-контур (replay/copilot polish)
2. **Push** — `7953b93` + `9f1afe3` ещё не пушили (только локально)
3. **КОММИТАС** — **не приоритет**: см. раздел «Верификация КОММИТАС» ниже

---

## Верификация КОММИТАС (20.06, после sync)

| Факт | Значение |
|------|----------|
| Источник «invite» | Устаревшая запись в `hh-negotiations-cache.json` (syncedAt **2026-05-25**, vacancy `133273123`) |
| После `sync-hh-responses` (20.06) | **0 invited**, КОММИТАС **нет** в кэше (20 актуальных откликов) |
| Baseline | `post-komitas-verify`: bucket E=0, declined=18, viewed=2 |

**Вывод:** готовиться к «интервью КОММИТАС» **не нужно** — это был артеfact старого кэша, не актуальное собеседование.

`config/hr-screening-answers.json` — универсальные ответы HR, не привязаны к КОММИТАС.

---

## Сделано перед паузой

| Срез | Статус |
|------|--------|
| Коммит v3.3.0 | `7953b93` — gate, Knowledge Store, dossier, chip, copilot, ops |
| employer-rag | `9f1afe3` — контекст dossier в письма + чат |
| Секреты Ollama | восстановлены из `farm-old` в `config/secrets.local.env` |
| employer-rag UI preview | в рабочей копии, **не закоммичено** |

### Коммиты (локально, не push)

```
9f1afe3 feat: employer-rag context for cover letters and chat replies
7953b93 feat: apply intelligence, knowledge store, employer dossier (v3.3.0)
840494d docs: handoff pause snapshot 2026-06-20
```

---

## Операционка (параллельно, без кода)

| Действие | Когда |
|----------|-------|
| «Утренний цикл» | **только вручную** (кнопка / CLI) — по расписанию не идёт |
| Вкладка **«Отkлики»** (левый «Раздел») | следить за статусами |
| Авто-подъём резюме | **выкл** (`config/resume-raise-schedule.json`) |
| Follow-up чатов | **выкл** (`config/chat-follow-up-schedule.json`) |
| Батч / «Перегенерировать» | **не обязательно** |

Дашборд: **http://127.0.0.1:3849**

---

## Проверено

```powershell
cd D:\Dev\apps\hh-ai
npm run test:employer-dossier
npm run test:employer-rag
npm run setup:check
node scripts/sync-hh-responses.mjs
```

---

## Не коммитить

- `config/secrets.local.env`, `.env`, `config/devops.env`
- `config/hr-screening-answers.json` (личный, в `.gitignore`)
- `config/resume-raise-schedule.json`, `config/chat-follow-up-schedule.json` (локальные, в `.gitignore`)
- `data/hr-video-analysis/`, `data/chat-send-launches.json`
- `data/hh-ai-knowledge.db`, очередь вакансий

---

## Следующий код

1. Закоммитить employer-rag UI preview (модалка письма + API `/api/cover-letter/employer-context`)
2. Интервью-контур — copilot / replay (без фокуса на КОММИТАС)
3. Push на remote — по явной просьбе

Предыдущий handoff: `docs/HANDOFF-2026-06-19.md`
