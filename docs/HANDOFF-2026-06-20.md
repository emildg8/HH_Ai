# Handoff — 20 июня 2026 (пауза: коммиты + employer-rag)

## С чего начать после паузы

Сказать агенту:

1. **Интервью КОММИТАС** — copilot, `config/hr-screening-answers.json`, подготовка к скринингу
2. **Продолжить план** — UI превью employer-rag в письме или интервью-контур (replay/copilot polish)
3. **Push** — `7953b93` + `9f1afe3` ещё не пушили (только локально)

---

## Сделано перед паузой

| Срез | Статус |
|------|--------|
| Коммит v3.3.0 | `7953b93` — gate, Knowledge Store, dossier, chip, copilot, ops |
| employer-rag | `9f1afe3` — контекст dossier в письма + чат |
| Секреты Ollama | восстановлены из `farm-old` в `config/secrets.local.env` |
| Git working tree | **чистый** (кроме локальных `data/`, не в git) |

### Коммиты (локально, не push)

```
9f1afe3 feat: employer-rag context for cover letters and chat replies
7953b93 feat: apply intelligence, knowledge store, employer dossier (v3.3.0)
```

---

## Операционка (параллельно, без кода)

| Действие | Когда |
|----------|-------|
| «Утренний цикл» | раз в день |
| Вкладка **«Отклики»** (левый «Раздел», не кнопка синхр. справа) | следить за статусами |
| КОММИТАС — приглашение | подготовка к интервью |
| Батч / «Перегенерировать» | **не обязательно** (на бою не планируете) |

Дашборд: **http://127.0.0.1:3849** (не 5545).

---

## Проверено

```powershell
cd D:\Dev\apps\hh-ai
npm run test:employer-dossier
npm run test:employer-rag
npm run setup:check
```

---

## Не коммитить

- `config/secrets.local.env`, `.env`, `config/devops.env`
- `config/hr-screening-answers.json` (личный, в `.gitignore`)
- `data/hr-video-analysis/`, `data/chat-send-launches.json`
- `data/hh-ai-knowledge.db`, очередь вакансий

---

## Следующий код (после возвращения)

1. UI read-only: превью employer-rag в модалке письма (опционально)
2. Интервью-контур — copilot / replay
3. Push на remote — по явной просьбе

Предыдущий handoff: `docs/HANDOFF-2026-06-19.md`
