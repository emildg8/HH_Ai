# Handoff — 18 июня 2026

Сводка на завтра. Сегодня работа остановлена; код в рабочем состоянии (тесты copilot зелёные).

## Главная задача завтра

**Единый модуль настроек** — 100% связка UI ↔ `preferences.json` ↔ localStorage.

План: [`.cursor/plans/единый_модуль_настроек_fb8c9101.plan.md`](../.cursor/plans/единый_модуль_настроек_fb8c9101.plan.md)

Сказать агенту:

> Делай план «Единый модуль настроек» — начни с registry + settings-store + вкладка Суфлёр.

## Что уже готово (суфлёр)

| Область | Статус |
|---------|--------|
| 4 вкладки copilot в хабе | готово |
| `teleprompter-prep.html` | готово |
| offer-guard, scaffold, Ctrl+Enter | готово |
| `npm run test:copilot` | 10 тестов OK |
| Демо 5 мин | [`DEMO-COPILOT-5MIN.md`](DEMO-COPILOT-5MIN.md) |

## Известный проблемный UX

- Блок «Суфлёр» в **Настройки → Профиль** (внизу) — пользователь его не видит на вкладке «Отбор»
- Микрофон/WASAPI в UI **не пишутся** в `config/preferences.json` (баг save)
- ~45% ключей `preferences.json` **без UI** (паттерны, llm weights, letter thresholds)

## Порядок работ завтра

1. `lib/settings-registry.mjs` + `test-settings-registry` (coverage всех ключей JSON)
2. `lib/settings-store.mjs` + `GET/PATCH /api/settings`
3. Вкладка **«Суфлёр»** (5-я в nav): select устройств, autosave, health pills
4. `dashboard/public/settings-hub.mjs` — единый `data-setting` + listeners
5. Остальные вкладки и «Эксперт» — сессия 2–3

## Быстрая проверка

```powershell
cd D:\Dev\apps\hh-ai
npm run test:copilot
npm run demo:copilot-preflight
```

## Связанные планы

- Аудит + конкуренты: `.cursor/plans/аудит_суфлёра_v1_6ce741ac.plan.md`
- UI волна F: `.cursor/plans/суфлёр_ui_ux_волна_f_e731c4b6.plan.md`

## Не сделано (осознанно отложено)

- Ручной smoke Zoom/Телемост
- Git commit copilot-среза (если нужен — попросить явно)
- Тег релиза v3.3.0
